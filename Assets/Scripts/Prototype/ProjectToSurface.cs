using UnityEngine;

public class ProjectToSurface : MonoBehaviour
{
	/* public MeshFilter filter;
	public Transform[] objects;
	
	const maxIteration = 2500;
	
	[ContextMenu("Project")]
	public void Project(){
		var filterPos = filter.transform.position;
		var rayDir = transform.up;
		
		var verts = filter.mesh.vertices;
		int vertIndex = 0;
		int interation = 0;
		
		foreach(var obj in objects){
			var vert = verts[vertIndex];
			var ray = new Ray(filterPos + vert, rayDir);
			
			bool raycast = Physics.Raycast(ray, out var hit, Mathf.Infinity);
			iteration = 0;
			
			while(!raycast & iteration < maxIteration){
				
				iteration ++;
			}
			
			if(){
				obj.position = hit.point;
				obj.up = hit.normal;
			}
				
			vertIndex ++;
			vertIndex = vertIndex % verts.Length;
			
			foreach(var vert in ){
				
				
				
			}
		} 
	}*/
}